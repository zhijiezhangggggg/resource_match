# 灾害应急资源匹配平台后端启动指南

## 🚀 快速启动

### 1. 环境要求
- **JDK**: 1.8 或更高版本
- **Maven**: 3.6 或更高版本
- **MySQL**: 8.0 或更高版本
- **IDE**: IntelliJ IDEA 或 Eclipse

### 2. 数据库准备

#### 2.1 创建数据库
```sql
-- 执行数据库脚本
source disaster_emergency.sql
```

#### 2.2 验证数据库
```sql
-- 检查表是否创建成功
USE disaster_emergency;
SHOW TABLES;

-- 检查测试数据
SELECT * FROM user;
SELECT * FROM organization;
SELECT * FROM resource;
```

### 3. 项目配置

#### 3.1 修改数据库配置
编辑 `src/main/resources/application.yml`:
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/disaster_emergency?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 你的数据库密码
```

#### 3.2 检查依赖
确保 `pom.xml` 中的依赖都已正确下载：
```bash
mvn clean install
```

### 4. 启动应用

#### 4.1 方式一：IDE启动
1. 打开 `EmergencyPlatformApplication.java`
2. 右键选择 "Run" 或点击运行按钮
3. 等待控制台输出 "Started EmergencyPlatformApplication"

#### 4.2 方式二：命令行启动
```bash
# 进入项目目录
cd resource_match_backend

# 编译项目
mvn clean compile

# 启动应用
mvn spring-boot:run
```

### 5. 验证启动

#### 5.1 检查端口
应用启动后会在 `8080` 端口运行，访问：
```
http://localhost:8080/api
```

#### 5.2 测试接口
使用Postman或curl测试基础接口：

**测试用户登录**:
```bash
curl -X POST http://localhost:8080/api/user/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

**测试文本解析**:
```bash
curl -X POST http://localhost:8080/api/disaster/parse-text \
  -H "Content-Type: application/json" \
  -d '{"originalText":"四川省成都市发生地震，需要100顶帐篷"}'
```

### 6. 功能验证

#### 6.1 核心功能测试流程

1. **用户登录** → 获取Token
2. **文本解析** → 测试灾情文本解析
3. **灾情上报** → 使用解析结果上报灾情
4. **需求提交** → 提交救援需求
5. **相似度计算** → 计算需求与资源的相似度
6. **获取推荐** → 获取推荐资源列表
7. **知识图谱** → 查看图谱节点和关系

#### 6.2 测试数据

系统已预置测试数据：
- **用户**: admin/admin123 (管理员), data_entry/data123 (数据录入员), test_user/test123 (普通用户)
- **机构**: 四川省应急管理厅、成都市消防救援支队、四川省红十字会、成都物资储备中心
- **资源**: 帐篷、食品、药品、饮用水、发电机、通讯设备

### 7. 常见问题

#### 7.1 数据库连接失败
```
Error: Could not create connection to database server
```
**解决方案**:
- 检查MySQL服务是否启动
- 验证数据库连接信息
- 确认数据库用户权限

#### 7.2 端口占用
```
Error: Port 8080 was already in use
```
**解决方案**:
- 修改 `application.yml` 中的端口号
- 或停止占用8080端口的其他服务

#### 7.3 依赖下载失败
```
Error: Failed to resolve dependencies
```
**解决方案**:
- 检查网络连接
- 配置Maven镜像源
- 清理Maven缓存: `mvn clean`

### 8. 开发建议

#### 8.1 日志查看
- 控制台日志：实时查看应用运行状态
- 日志文件：`logs/admin-system.log`

#### 8.2 数据库操作
- 使用Druid监控：`http://localhost:8080/api/druid`
- SQL日志：已在配置中开启，可在控制台查看

#### 8.3 API测试
- 使用Postman导入API文档
- 或使用Swagger UI（如果配置了）

### 9. 部署说明

#### 9.1 打包应用
```bash
mvn clean package
```

#### 9.2 运行JAR包
```bash
java -jar target/admin-system-1.0.0.jar
```

#### 9.3 生产环境配置
- 修改数据库连接为生产环境
- 配置JWT密钥
- 设置日志级别
- 配置CORS策略

### 10. 技术支持

如遇到问题，请：
1. 查看控制台错误信息
2. 检查日志文件
3. 参考API接口文档
4. 联系开发团队

---

## 📋 功能清单

✅ **已完成功能**:
- [x] 用户管理和认证
- [x] 灾情信息管理
- [x] 需求信息管理
- [x] 资源信息管理
- [x] 知识图谱构建
- [x] 文本解析功能
- [x] 相似度计算
- [x] 智能匹配推荐
- [x] 匹配调度管理
- [x] 统计分析功能
- [x] API接口文档

🎯 **核心特性**:
- 基于知识图谱的智能匹配
- 多维度相似度计算
- 文本自动解析
- RESTful API设计
- 完整的业务闭环

现在你可以开始使用这个完整的灾害应急资源匹配平台后端系统了！
